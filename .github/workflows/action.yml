name: ci

on:
  push:
    branches:
      - "dockerize"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        with:
          context: .
          file: ./Dockerfile
          push: false
